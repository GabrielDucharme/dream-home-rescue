# Database connection string
DATABASE_URI=mongodb://127.0.0.1/your-database-name

# Or use a PG connection string
#DATABASE_URI=postgresql://127.0.0.1:5432/your-database-name

# Used to encrypt JWT tokens
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Used to configure CORS, format links and more. No trailing slash
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Secret used to authenticate cron jobs
CRON_SECRET=YOUR_CRON_SECRET_HERE

# Used to validate preview requests
PREVIEW_SECRET=YOUR_SECRET_HERE

# Stripe secret key
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# Stripe public key for client-side
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Stripe webhook endpoint secret (from Stripe CLI or Dashboard)
# For local development with Stripe CLI, run 'stripe listen --forward-to localhost:3000/api/stripe/webhooks'
# and use the webhook signing secret it provides
STRIPE_WEBHOOKS_ENDPOINT_SECRET=whsec_your_stripe_webhook_secret
